input {
		file {
				type => "csv"
				path => "/tmp/datasets/training/normalTrafficTraining.csv"
				start_position => "beginning"
				}
	   }

filter {

		}

output {
	elasticsearch {
		action => "index"
	 	index => "logs-training"
		manage_template => true
		hosts =>  ["http://elasticsearch:9200"]
	}
}